═══════════════════════════════════════════════════════════════
  SBOM Dependency Finder - macOS Build Checklist
═══════════════════════════════════════════════════════════════

REQUIREMENTS: Apple Silicon Mac (M1/M2/M3) only

Follow these steps on your Mac to build the executable for Mac users.

STEP 1: Get the latest code
───────────────────────────────────────────────────────────────
Open Terminal and run:

    git clone https://github.com/Aidan-vG/SBOM_Dependency_Finder.git
    cd SBOM_Dependency_Finder

Or if you already have it:

    cd SBOM_Dependency_Finder
    git pull


STEP 2: Install dependencies
───────────────────────────────────────────────────────────────
    npm install


STEP 3: Build the executables
───────────────────────────────────────────────────────────────
    npm run build:exe

Expected output:
  ✓ Compiled TypeScript
  ✓ Bundled application
  ✓ Generated SEA blob
  ✓ Created bin/sbom-finder-macos
  ✓ Code-signed executable
  ✓ Set executable permission


STEP 4: Test the executable
───────────────────────────────────────────────────────────────
    ./bin/sbom-finder-macos --help
    ./bin/sbom-finder-macos info test/fixtures/sample-sbom.json

✓ Should work WITHOUT needing to run chmod +x


STEP 5: Package for distribution
───────────────────────────────────────────────────────────────
    npm run package

This creates:
  • sbom-finder-macos-v0.1.0.zip

The .zip format preserves executable permissions!


STEP 6: Transfer back to Windows
───────────────────────────────────────────────────────────────
Copy these files to your Windows machine:

    bin/sbom-finder-macos            (macOS executable)
    sbom-finder-macos-v0.1.0.zip     (Distribution package)

Methods to transfer:
  • Git: commit to a branch and pull on Windows
  • Cloud: Upload to OneDrive/Dropbox
  • USB: Copy directly
  • Network: Use shared folder


TROUBLESHOOTING
───────────────────────────────────────────────────────────────
"node: command not found"
  → Install Node.js: brew install node

"npm: command not found"
  → Reinstall Node.js (npm comes with it)

Code signing fails
  → The executable will still work but users may need to:
    - Right-click → Open (first time only)
    - Or run: xattr -cr sbom-finder-macos

Build succeeds but executable doesn't run
  → Make sure you're on Apple Silicon (M1/M2/M3)
  → Check architecture: uname -m (should show "arm64")
  → Intel Macs are not supported


═══════════════════════════════════════════════════════════════
For detailed documentation, see:
  • QUICKSTART_MAC.md - Quick reference
  • BUILD_MACOS.md - Full guide with troubleshooting
═══════════════════════════════════════════════════════════════
